package com.example.javafx.main;

import com.airhacks.afterburner.views.FXMLView;

public class MainView extends FXMLView {
    // This class make relation between Presenter and FXML automaticall,
    // additional to allow CDI injection (using @Inject) into Controllers.
    // Usually, it's implementation is nothing.
}
